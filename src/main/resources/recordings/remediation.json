{ "agent": "remediation", "phase": "main", "captured": "authored",
  "turns": [
    { "when": "start",
      "say": "I have an approved operation. Before changing anything I will run the pre-flight check against the live catalog.",
      "calls": [ { "as": "preflight", "tool": "preflight_check", "args": { "operation_ref": "$op" } } ] },
    { "when": "ok:preflight_check",
      "say": "Pre-flight passed: the role still matches the fingerprint the plan was built on. Applying the approved plan by reference.",
      "calls": [ { "as": "apply", "tool": "apply_right_size_role", "args": { "operation_ref": "$op" } } ] },
    { "when": "ok:apply_right_size_role",
      "say": "The change and its ledger marker committed together. The operation is applied.",
      "decision": { "decision": "applied", "reason": "The approved plan was applied once and the ledger row confirms it.", "evidence": ["$ev:apply"] } },
    { "when": "error:apply_right_size_role:TIMEOUT",
      "say": "No reply arrived from the database. I do not know whether the change committed, so I will not retry. I will ask for the operation's status instead.",
      "calls": [ { "as": "status", "tool": "get_operation_status", "args": { "operation_ref": "$op" } } ] },
    { "when": "error:apply_right_size_role:RECONCILE_REQUIRED",
      "say": "The broker refuses to apply while the outcome is unknown. Checking status.",
      "calls": [ { "as": "status", "tool": "get_operation_status", "args": { "operation_ref": "$op" } } ] },
    { "when": "error:apply_right_size_role:ABORTED_BEFORE_COMMIT",
      "say": "The database reported an error before commit, so nothing changed. This is safe to retry with the same operation.",
      "calls": [ { "as": "apply", "tool": "apply_right_size_role", "args": { "operation_ref": "$op" } } ] },
    { "when": "error:apply_right_size_role:LOCK_TIMEOUT",
      "say": "The target lock was busy. Nothing changed. Retrying the same operation.",
      "calls": [ { "as": "apply", "tool": "apply_right_size_role", "args": { "operation_ref": "$op" } } ] },
    { "when": "ok:get_operation_status:applied",
      "say": "The ledger holds a row for this operation and the live fingerprint equals its after-state. The change was applied exactly once.",
      "decision": { "decision": "applied", "reason": "Reconciliation found the ledger row; the change committed before the reply was lost.", "evidence": ["$ev:status"] } },
    { "when": "ok:get_operation_status:not_applied",
      "say": "No ledger row and the before-state is intact, so the change never committed. Retrying the same operation.",
      "calls": [ { "as": "apply", "tool": "apply_right_size_role", "args": { "operation_ref": "$op" } } ] },
    { "when": "ok:get_operation_status:in_flight",
      "say": "A write is still holding the lock. Asking again.",
      "calls": [ { "as": "status", "tool": "get_operation_status", "args": { "operation_ref": "$op" } } ] } ] }
